<odoo>
    <record id="view_sale_order_template_form_docs" model="ir.ui.view">
        <field name="name">sale.order.template.form.docs</field>
        <field name="model">sale.order.template</field>
        <field name="inherit_id" ref="sale.view_sale_order_template_form"/>
        <field name="arch" type="xml">
        <xpath expr="//sheet/notebook" position="inside">
            <page string="Documentos">
            <field name="quote_doc_ids" context="{'default_template_id': active_id}">
                <tree editable="bottom">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="attachment_id"/>
                <field name="default_checked"/>
                </tree>
                <form>
                <group>
                    <field name="sequence"/>
                    <field name="name"/>
                    <field name="attachment_id"/>
                    <field name="default_checked"/>
                </group>
                </form>
            </field>
            </page>
        </xpath>
        </field>
    </record>
</odoo>
