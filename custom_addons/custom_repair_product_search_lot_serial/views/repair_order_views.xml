<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_repair_order_form_inherit" model="ir.ui.view">
        <field name="name">repair.order.form.inherit</field>
        <field name="model">repair.order</field>
        <field name="inherit_id" ref="repair.view_repair_order_form"/>
        <field name="arch" type="xml">
            <!-- Campo computed invisible para dominios -->
            <xpath expr="//field[@name='product_id']" position="before">
                <field name="allowed_lot_ids" invisible="1"/>
            </xpath>
            
            <!-- Asegurar que lot_id sea visible y editable -->
            <xpath expr="//field[@name='lot_id']" position="attributes">
                <attribute name="string">📋 Lote/Serie</attribute>
                <attribute name="invisible">0</attribute>
                <attribute name="readonly">0</attribute>
            </xpath>
            
            <!-- Añadir campo x_machine_number después de lot_id -->
            <xpath expr="//field[@name='lot_id']" position="after">
                <field name="x_machine_number" string="📋 Número de Máquina" readonly="0" /> <!-- Cambiado a readonly=0 para depurar -->
                
                <!-- Asegurar que el campo cantidad esté alineado -->
                <xpath expr="//field[@name='product_uom_qty']" position="attributes">
                    <attribute name="colspan">1</attribute>
                </xpath>
                
                <!-- Hacer product_id readonly cuando esté lleno -->
                <xpath expr="//field[@name='product_id']" position="attributes">
                    <attribute name="readonly">1</attribute>
                    <attribute name="force_save">1</attribute>
                </xpath>
            </xpath>
        </field>
    </record>
</odoo>